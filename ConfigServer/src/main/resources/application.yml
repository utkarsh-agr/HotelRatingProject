server:
  port: 8087

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/utkarsh-agr/config-server_HotelRating
          clone-on-start: true

